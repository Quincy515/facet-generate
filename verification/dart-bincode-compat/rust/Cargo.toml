[package]
name = "bincode-smoke-test"
version = "0.1.0"
edition = "2021"

[dependencies]
# English: Match Crux exactly — bincode 1.3 with fixint_encoding
# 中文:和 Crux 完全对齐 —— bincode 1.3 + fixint_encoding
bincode = "=1.3"
serde = { version = "1", features = ["derive"] }

[[bin]]
name = "emit"
path = "src/main.rs"
